describe('eventDatePicker', function(){
    beforeEach(function(){
        // concerned that fixture is large and involved and creating too complex a seam
        setFixtures('');
    });

    xit('hides event repeat options by default').pend('awaiting refactor of related form for use in fixture');
    xit('shows event repeat options when event is set to repeat').pend('awaiting refactor of related form for use in fixture');
    xit('shows event repeat optional ending when event is set to end').pend('awaiting refactor of related form for use in fixture');
    xit('hides event repeat optional ending when event is set to never end').pend('awaiting refactor of related form for use in fixture');
});