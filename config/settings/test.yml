features:
  slack:
    notifications:
      enabled: false
    invites:
      enabled: false
  twitter:
    hangout_notifications:
      enabled: false

mailer:
  delivery_method: 'test'

privileged_users: 'chesa@example.com, random@random.com' 

twitter:
  consumer_key:        nil
  consumer_secret:     nil
  access_token:        nil
  access_token_secret: nil
