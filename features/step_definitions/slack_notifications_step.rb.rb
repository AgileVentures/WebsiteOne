Given(/^that Slack Notifications are enabled$/) do
  pending
  # env var "SLACK_NOTIFICATIONS" is set to true
end

And(/^that the Agile Bot URL is set correctly$/) do
  pending
  # env var "AGILE_BOT_URL" is set to "http://agilebot.herokuapp.com"
end

When(/^I start a hangout$/) do
  pending
  # not practical to really start hangout - but we could
  # simulate incoming connection from hangout plugin
end

Then(/^there should be an appropriate notification in slack$/) do
  pending
  # can stub here
end