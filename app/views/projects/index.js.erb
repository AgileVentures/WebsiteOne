$('.pagination').replaceWith('<%= j will_paginate @projects %>');
$('#project-list').html("<%= escape_javascript(render 'filtered_project_list') %>");
$('#projects-header').html("<%= "#{@language} Projects" %>")
