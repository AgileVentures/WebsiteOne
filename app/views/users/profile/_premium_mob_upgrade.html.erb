<% if presenter.user_same_as?(current_user) %>
    <% button_text = 'Upgrade to Premium Mob' %>
    <%= form_tag subscription_path, method: "put" do %>
        <input type="hidden" name="user_id" value="<%=presenter.user.slug %>">
        <%= submit_tag button_text, class:"btn btn-primary btn-lg active",
                                    style: "margin: 5px;",
                                    title: 'Premium Mob is a £25 a month subscription to support the AgileVentures charity, that gives you access to group coding sessions with an AgileVentures Mentor and professional development planning support'%>
    <% end %>
<% end %>


